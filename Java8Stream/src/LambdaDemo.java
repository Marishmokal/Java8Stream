
public class LambdaDemo {
public static void main(String[] args) {
	
	//RULES FOR LAMBDA EXPRESSION
	/*
	 * // It is noting but the advance method // no need to provide mehtod
	 * name,return type,modifiers
	 *  if multiple lines are there the need to provide curly braces 
	 *  no need to provide datatype
	 */
	//within curly braces we must write return stmt
	//without curly braces no need to provide return stmt
	
	
	
	
	//Functional interface
	/*
	 * interface which contain only one abstract method is called as functional
	 * interface Callable (call)and Runnable(Run) are functional interfaces
	 * 
	 * @FunctionalInterface annotaton is used
	 * 
	 * For calling lambda expression we must use functional interface
	 */
	
//	Eg is My Inteface
}
}
